{
  "name": "bureau_cypress",
  "version": "1.0.0",
  "description": "Automação do sistema Bureau - Idea Maker",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run --config video=true --env allure=true",
    "allure:generate": "allure generate allure-results --clean",
    "allure:open": "allure open allure-report",
    "apagar:resultado": "rm -r allure-results",
    "apagar:report": "rm -r allure-report",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Rafael Igreja",
  "license": "Idea Maker",
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "@faker-js/faker": "^8.0.2",
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "cpf": "^2.0.1",
    "cypress": "^12.17.3",
    "cypress-file-upload": "^5.0.8"
  },
  "dependencies": {
    "cypress-xpath": "^2.0.1"
  }
}
